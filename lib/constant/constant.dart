import 'package:flutter/material.dart';

const defautfontsize = 14.0;
const primaryColor = Color(0xFF1FC44B);
const linnear = LinearGradient(
    colors: [Color(0xFF9EFFB8), Color(0xFF1FC44B)],
    begin: Alignment.centerLeft,
    end: Alignment.centerRight,
    tileMode: TileMode.decal);
